# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: enforce-fallback-config-scaledobjects
  annotations:
    policies.kyverno.io/title: Enforce Fallback config for ScaledObjects
    policies.kyverno.io/category: Best Practices
    policies.kyverno.io/severity: medium
    policies.kyverno.io/minversion: 1.3.0
    policies.kyverno.io/subject: ScaledObject
    policies.kyverno.io/description: >-
      ScaledObjects should have a fallback configuration to ensure that they can handle cases
      where a scaler isn't working in a predictable way.
spec:
  validationFailureAction: Audit
  background: true
  rules:
  - name: check-fallback-config
    match:
      resources:
        kinds:
        - keda.sh/v1alpha1/ScaledObject
    validate:
      message: >-
        ScaledObjects must have a fallback config defined.
      pattern:
        spec:
          fallback:
            failureThreshold: "?*"
            replicas: "?*"
