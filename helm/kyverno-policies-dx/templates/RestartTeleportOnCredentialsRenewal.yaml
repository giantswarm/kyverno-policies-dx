# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: restart-teleport-on-secrets-change
  annotations:
    policies.kyverno.io/title: Restart Teleport Components on Secrets Change
    policies.kyverno.io/category: Best Practices
    policies.kyverno.io/severity: low
    policies.kyverno.io/minversion: 1.6.0
    policies.kyverno.io/subject: Deployment, StatefulSet, Secret, ConfigMap
    policies.kyverno.io/description: >-
      This policy automatically triggers rolling restarts of teleport-kube-agent pods when
      credentials are renewed, and cleans up stale state secrets when configuration changes.
      The deployment/statefulset restarts when the join token secret is updated, and state
      secrets are deleted when the ConfigMap is modified to ensure fresh state on restart.
spec:
  background: false
  schemaValidation: false
  rules:
    - name: restart-teleport-kube-agent-on-credentials-change
      match:
        any:
        - resources:
            kinds:
            - Secret
            names:
            - teleport-kube-agent-join-token
            namespaces:
            - kube-system
            operations:
            - UPDATE
      preconditions:
        all:
        - key: "{{ `{{` }}request.object.data{{ `}}` }}"
          operator: NotEquals
          value: "{{ `{{` }}request.oldObject.data{{ `}}` }}"
      mutate:
        targets:
          - apiVersion: apps/v1
            kind: Deployment
            name: teleport-kube-agent
            namespace: kube-system
          - apiVersion: apps/v1
            kind: StatefulSet
            name: teleport-kube-agent
            namespace: kube-system
        patchStrategicMerge:
          spec:
            template:
              metadata:
                annotations:
                  teleport-operator.giantswarm.io/restarted-at: "{{ `{{` }}time_now(){{ `}}` }}"
                  teleport-operator.giantswarm.io/credentials-version: "{{ `{{` }}request.object.metadata.resourceVersion{{ `}}` }}"
                  teleport-operator.giantswarm.io/restart-reason: "credentials-rotation"

    - name: mark-state-secrets-for-cleanup-on-config-change
      match:
        any:
        - resources:
            kinds:
            - ConfigMap
            names:
            - teleport-kube-agent
            namespaces:
            - kube-system
            operations:
            - UPDATE
      preconditions:
        all:
        - key: "{{ `{{` }}request.object.data{{ `}}` }}"
          operator: NotEquals
          value: "{{ `{{` }}request.oldObject.data{{ `}}` }}"
      mutate:
        targets:
          - apiVersion: v1
            kind: Secret
            name: "*-state"
            namespace: kube-system
        patchStrategicMerge:
          metadata:
            labels:
              teleport.giantswarm.io/cleanup-state: "{{ `{{` }}time_now_utc(){{ `}}` }}"

---
apiVersion: kyverno.io/v2
kind: ClusterCleanupPolicy
metadata:
  name: cleanup-teleport-state-secrets
spec:
  schedule: "*/5 * * * *"
  conditions:
    all:
    - key: "{{ `{{` }}target.metadata.labels.\"teleport.giantswarm.io/cleanup-state\" || ''{{ `}}` }}"
      operator: NotEquals
      value: ""
  match:
    any:
    - resources:
        kinds:
        - Secret
        names:
        - "teleport-kube-agent-*-state"
        namespaces:
        - kube-system

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    application.giantswarm.io/team: honeybadger
    rbac.kyverno.io/aggregate-to-background-controller: "true"
  name: kyverno:gs-dx:restart-teleport-on-secrets-change
rules:
- apiGroups:
  - apps
  resources:
  - deployments
  - statefulsets
  verbs:
  - get
  - list
  - patch
  - update
- apiGroups:
  - ""
  resources:
  - secrets
  - configmaps
  verbs:
  - get
  - list
  - watch
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    application.giantswarm.io/team: honeybadger
    rbac.kyverno.io/aggregate-to-cleanup-controller: "true"
  name: kyverno:gs-dx:cleanup-teleport-state-secrets
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - delete
